# Tasks

> **Superseded.** This file stopped being maintained mid-Phase-1 while `plan.md`
> ran to Phase 5, and it referenced three artefacts that never existed
> (`gemini.md`, `implementation_plan.md`, and a `cmd/cli` package). It is kept
> only so the references above resolve to an explanation rather than a dead end.
>
> Current work is tracked in `CHANGELOG.md` (what shipped) and the phased
> roadmap agreed with the maintainer (what is next).
